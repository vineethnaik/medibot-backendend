# EC2 environment – copy to /opt/medibots-backend/.env
# chmod 600 .env
# For Aurora MySQL: use cluster endpoint, port 3306, add ?useSSL=true&serverTimezone=UTC

SPRING_PROFILES_ACTIVE=production
SPRING_DATASOURCE_URL=jdbc:mysql://YOUR_AURORA_CLUSTER_ENDPOINT:3306/medibot?useSSL=true&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=admin
SPRING_DATASOURCE_PASSWORD=your_password
JWT_SECRET=your-256-bit-secret
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com
SERVER_ADDRESS=0.0.0.0
PORT=8080
